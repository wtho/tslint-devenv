{
  "name": "tslint-env",
  "version": "0.1.0",
  "description": "Development environment for TSLint",
  "main": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "rule": "node --inspect-brk -r ts-node/register node_modules/.bin/tslint -r src",
    "build": "tsc -p tsconfig.json"
  },
  "author": "Thomas Wirth <thomas@wtho.de>",
  "license": "MIT",
  "dependencies": {
    "ts-node": "^8.2.0",
    "tslint": "^5.17.0",
    "typescript": "^3.5.1"
  },
  "devDependencies": {
    "@types/node": "^12.0.7"
  }
}
